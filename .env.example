# =============================================================================
# 環境變數範本：複製這個檔案為 .env 後填入實際值
# =============================================================================
#
#     cp .env.example .env
#     open -e .env       # macOS 用文字編輯器開啟
#
# .env 已加入 .gitignore，所以你填進去的 API key 不會被 commit 到 git。
# 你不會用到的 provider 直接保留註解（前面有 # 的那行）即可。
#
# Provider 與 model 的選擇是在 scripts/ai_config.yaml 裡設定，這裡只放 key。
# =============================================================================


# --- Ollama（本地、預設、不需要 key） ---
# 只要 Ollama 已經在跑（http://localhost:11434），這個區塊就不用改。
OLLAMA_HOST=http://localhost:11434


# --- Google Gemini（免費 tier，推薦） ---
# 申請：https://aistudio.google.com  → 左邊 API Keys → Create API key
# 不需要綁信用卡，個人用免費配額足夠。
# GEMINI_API_KEY=AIzaSy...


# --- Anthropic Claude（付費，最高品質） ---
# 申請：https://console.anthropic.com（需要綁信用卡）
# Claude Max 訂閱不包含 API 額度，需另外付費。
# ANTHROPIC_API_KEY=sk-ant-...


# --- OpenRouter（聚合多家模型） ---
# 申請：https://openrouter.ai（最低儲值 5 美元）
# 一個 key 可用 Claude、Gemini、Llama 等幾百種模型。
# OPENROUTER_API_KEY=sk-or-...
